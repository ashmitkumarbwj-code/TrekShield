<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.treksafetyapp">

    <!-- Permissions Required by the App -->
    <!-- Internet & Network state for offline maps download and Weather API -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- Location Permissions for GPS -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION"/>
    
    <!-- Critical permissions for Dead Man's Switch -->
    <uses-permission android:name="android.permission.SEND_SMS" />
    
    <!-- Service and background permissions -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TrekSafetyApp"> <!-- Or whichever theme you use -->
        
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Our location tracking service running transparently to the user -->
        <service 
            android:name=".service.TrekTrackingService"
            android:foregroundServiceType="location"
            android:exported="false" />

        <receiver android:name=".utils.SmsStatusReceiver" android:exported="true">
            <intent-filter>
                <action android:name="SMS_SENT_ACTION"/>
                <action android:name="SMS_DELIVERED_ACTION"/>
            </intent-filter>
        </receiver>
            
    </application>
</manifest>
